define servicegroup {
<% %W(servicegroup_name alias members servicegroup_members notes notes_url action_url
   ).each do |key| %>
<% if cc.has_key?(key) && !cc[key].to_s.empty? %>
  <%= key %>    <%= cc[key].to_s %>
<% end %>
<% end %>
}
