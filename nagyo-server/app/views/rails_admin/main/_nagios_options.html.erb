<%= form.select field.method_name, field.enum,
  { :include_blank => false, :selected => field.value.split(',')},
  field.html_attributes.reverse_merge({ :data => { :multi_enumeration => true }, :multiple => true }) %>

